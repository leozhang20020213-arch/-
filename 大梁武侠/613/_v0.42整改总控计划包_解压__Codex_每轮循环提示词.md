# Codex每轮循环提示词

请执行下一轮90分钟整改循环。

固定步骤：

1. 读取 `progress_state.json`。
2. 打开 `大梁武侠TRPG_v0.42整改总控计划包.xlsx`。
3. 检查上一轮任务的交付物和质量闸门。
4. 若上一轮未通过，先修复上一轮。
5. 若上一轮通过，选择下一个P0且依赖满足的任务。
6. 执行本轮任务，最多只完成一个任务或一个明确子任务。
7. 所有结论必须给证据路径或日志路径。
8. 更新状态文件。
9. 输出简报：

- 本轮完成：
- 输出文件：
- 验收结果：
- 发现问题：
- 阻塞项：
- 下一轮建议：
