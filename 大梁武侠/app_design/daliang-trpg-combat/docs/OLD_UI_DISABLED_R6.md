# R5 UI Disabled

R5 is obsolete for the R6 card-operation rollback.

The legacy React/Phaser prototype is retained for reference only. Its default
`npm run dev` entry has been disabled so it is not mistaken for the active UI.

Use the new Godot project instead:

```text
D:\trpg\大梁武侠\app_design\daliang-trpg-godot-r6
```

Legacy preview remains available with:

```text
npm run dev:legacy
```

Backup copy:

```text
D:\trpg\大梁武侠\app_design\ui_backups\R5_disabled_20260620_daliang-trpg-combat
```
